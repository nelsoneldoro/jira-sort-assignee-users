# jira-sort-assignee-users

### Generating release:

- 1 Run `$yarn version [--patch | --minor | --patch]`
  - See more about [yarn version](https://classic.yarnpkg.com/lang/en/docs/cli/version/)
- 2 Upload the generated `extension-v${version}.zip` file.
